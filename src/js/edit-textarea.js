$(function() {
    $('.field__input-textarea').on('input', function(e) {
        // let text = $(e.target).text();
        // if (!text) text = '';
        // const $textarea = $(e.target).parent().find('textarea')
        // $textarea.val(text);

        //$(e.target).parent().find('textarea')[0].value = $(e.target)[0].innerText;
    })
});